# Local editor and OS files
.vscode/
.DS_Store
Thumbs.db

# Local tooling (portable Node used only for running unit tests)
.tools/

# Local runtime exports
*.ics
*.json

# 本資料夾的資料檔（serve.cmd 模式；每個資料夾各自一份，含真實資料，絕不提交）
local-state.json
local-state.json.tmp

# Local-only reference material (never pushed)
zz_requests/
